<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">#e77f00</item>
    </style>

    <style name="AppTheme.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item> <!-- Text color of the button -->
        <item name="backgroundTint">@color/btn_color_selector_night</item> <!-- Background color of the button -->
    </style>

    <style name="AppTheme.TextInput" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/white</item> <!-- Text color of the button -->
        <item name="backgroundTint">@color/dark_gray</item> <!-- Background color of the button -->
    </style>

    <style name="AppTheme.Thumb" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="thumbTint">@color/background_orange</item>
        <item name="trackTint">@color/switch_colors</item>
        <item name="android:backgroundTint">@color/dark_gray</item>
    </style>

    <style name="AppTheme.Logo">
        <item name="android:src">@drawable/logo_night</item>
    </style>
</resources>
