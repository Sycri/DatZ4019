<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DatZ4019" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_orange</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/main_orange</item>
        <item name="buttonStyle">@style/Theme.DatZ4019.Button</item>
    </style>

    <style name="Theme.DatZ4019.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item> <!-- Text color of the button -->
        <item name="backgroundTint">@color/white</item> <!-- Background color of the button -->
    </style>
</resources>