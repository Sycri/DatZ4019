<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background_orange</item>
    </style>

    <style name="AppTheme.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/black</item> <!-- Text color of the button -->
        <item name="backgroundTint">@color/btn_color_selector</item> <!-- Background color of the button -->
    </style>

    <style name="AppTheme.TextInput" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/black</item> <!-- Text color of the button -->
        <item name="backgroundTint">@color/white</item> <!-- Background color of the button -->
    </style>

    <style name="AppTheme.Frame">
        <item name="android:backgroundTint">@color/white</item> <!-- Background color of the button -->
    </style>

    <style name="AppTheme.OrderCard">
        <item name="android:textColor">@color/black</item> <!-- Text color of the button -->
        <item name="android:backgroundTint">@color/white</item> <!-- Background color of the button -->
    </style>

    <style name="AppTheme.Thumb" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="thumbTint">@color/background_orange</item>
        <item name="trackTint">@color/switch_colors</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="AppTheme.Logo">
        <item name="android:src">@drawable/main_logo</item>
    </style>

    <style name="AppTheme.AddLogo">
        <item name="android:src">@drawable/plus_light</item>
    </style>

    <style name="AppTheme.TimePicker" parent="ThemeOverlay.Material3.MaterialTimePicker">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorPrimaryInverse">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorSecondaryInverse">@color/black</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColorTertiary">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="colorControlNormal">@color/black</item>
    </style>



</resources>
